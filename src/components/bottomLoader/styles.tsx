import { StyleSheet } from "react-native";

export default StyleSheet.create({
    container: { 
        elevation: 10, 
        alignItems: "center", 
        position: 'absolute', 
        bottom: 10 
    },
});